interface Addresses {
  btcAddress: string;
  solAddress: string;
  dogeAddress: string;
  dianaAddress: string;
}

const fetchAddresses = (props: Addresses): Addresses => {
  return props;
};

export default fetchAddresses;
